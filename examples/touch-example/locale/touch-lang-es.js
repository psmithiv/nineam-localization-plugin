Ext.onReady(function() {
    var cm = Ext.ClassManager,
        exists = Ext.Function.bind(cm.get, cm);

    Ext.define("Touch.locales.es.Global", {
        title: 'es.title',
        btnWidth: 100,
        comp: {
            title: 'es.comp.title'
        }
    });
});
